/**
 * TODO: Do everything inside curried function:
 * Iterate over subProcessors object
 * Call every field with "data" argument
 * Create new object where field matches its function result
 *
 * Remember: this function should only be allowed
 *  to take an object with function as value
 */
export function createProcessor(subProcessors) {
  return (data: any) => {};
}
